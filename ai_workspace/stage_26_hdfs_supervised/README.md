# Stage 26 — HDFS Supervised Evaluation

## Purpose: supervised evaluation on HDFS-style labeled data (reference benchmark dataset).

## Outputs:
- Confusion matrix + ROC/PR curves for HDFS validation (PNG files)
- Supervised evaluation results for v1/v2 flows

## Scripts:
- `run_hdfs_supervised_v1.py`
- `run_hdfs_supervised_v2.py`