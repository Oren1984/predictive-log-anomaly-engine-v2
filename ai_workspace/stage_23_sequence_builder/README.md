# Stage 23 — Sequence Builder

## Purpose: convert structured events into sequences/windows for anomaly detection models.

## Outputs:
- Sequence datasets (e.g., sliding windows) used by baseline and transformer models.

## Variants:
- `run_sequence_builder.py`
- `run_sequence_builder_v2.py` (improved/updated pipeline)

## Run:
- Choose one of the scripts above based on the current pipeline version.